1. download
sudo apt-get install apache2

2.apache address:
Apache keeps its main configuration files within the "/etc/apache2" folder:
cd /etc/apache2

3.apache config
gksu gedit /etc/apache2/sites-available/000-default.conf

add to 000-default.conf:

<VirtualHost *:80>
	# The ServerName directive sets the request scheme, hostname and port that
	# the server uses to identify itself. This is used when creating
	# redirection URLs. In the context of virtual hosts, the ServerName
	# specifies what hostname must appear in the request's Host: header to
	# match this virtual host. For the default virtual host (this file) this
	# value is not decisive as it is used as a last resort host regardless.
	# However, you must set it for any further virtual host explicitly.
	#ServerName www.example.com

	ServerAdmin webmaster@localhost
	DocumentRoot  /home/matrix/workspace/www
    <Directory />
                Options FollowSymLinks
                AllowOverride None
    </Directory>
    <Directory /home/matrix/workspace/www/>
                Options Indexes FollowSymLinks MultiViews
                AllowOverride None
                Order allow,deny
                allow from all
    </Directory>


4.
sudo /etc/init.d/apache2 start   #start apache
sudo /etc/init.d/apache2 stop   #stop apache
sudo /etc/init.d/apache2 restart   #restart apache

5. reference form wiki
https://acseng.us.oracle.com/download/attachments/56657894/httpd.conf?version=1&modificationDate=1424980804614&api=v2
https://acseng.us.oracle.com/pages/viewpage.action?pageId=56657894&focusedCommentId=59935654#comment-59935654
https://acseng.us.oracle.com/display/ACSeng/Set+up+ACSPortal+Env.+and+add+CSF+module+to+ACSPortal
https://acseng.us.oracle.com/download/attachments/59938171/httpd.conf?version=1&modificationDate=1428539560676&api=v2
https://acseng.us.oracle.com/pages/viewpage.action?pageId=59938171







